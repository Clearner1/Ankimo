services:
  ankimo:
    build: .
    container_name: ankimo
    ports:
      - "3000:80"
    volumes:
      - ./.htpasswd:/etc/nginx/.htpasswd:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
