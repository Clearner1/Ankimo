# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

Ankimo 首先服务于一位长期使用 Anki 的个人知识工作者。用户会在电脑或移动浏览器中快速记下一段短笔记、摘录、想法，或创建一张问答卡片；随后通过标签整理内容，并在需要集中学习时使用 Anki 旗标建立临时队列。

## Product Purpose

Ankimo 把 Flomo 式的低摩擦记录体验与 Anki 的卡片、标签、搜索和复习能力放在同一个网页界面中。成功意味着用户可以快速捕捉内容、方便地补充标签和再次查找，同时保留 Anki 作为数据与复习系统。

## Positioning

Ankimo 不是独立笔记数据库，也不是按牌组组织的 Anki 外壳。它直接通过 AnkiConnect 操作用户本地的 Anki 数据，以标签作为长期组织和筛选单位，并将牌组降为存储或兼容层。短笔记与问答卡片共享记录入口，但具有明确不同的复习语义。

## Operating Context

- Anki 桌面端及 AnkiConnect 需要在用户本地运行；远程网页访问仍映射回这台本地设备，而不是依赖一台没有 Anki 的远程服务器。
- 用户先在网页中记录内容，再逐步为每张笔记补充标签；浏览、搜索和分组以标签为主。
- 红旗、橙旗、绿旗是临时学习队列。用户常按红旗、橙旗、绿旗的顺序浏览一遍，再回到 Anki 进行正式复习。
- 牌组仍是 Anki 的底层概念，但不是 Ankimo 的主要信息架构。短笔记统一进入 `Ankimo` 牌组；问答卡片可使用用户选择的牌组和模板。

## Capabilities and Constraints

- 支持创建、浏览、搜索、编辑和删除 Anki 笔记，并显示层级标签树、置顶标签、三色旗标、牌组、统计和复习热力图。
- 默认记录模式是短笔记；短笔记保存到 `Ankimo` 牌组后立即暂停，不进入日常复习。
- 问答模式创建正常参与 Anki 复习的卡片，并保留牌组及模板选择能力。
- 标签是主要分组、筛选和回访入口；牌组相关能力应保留，但可以作为次级或按需展开的控制。
- 仅保留红旗、橙旗、绿旗三种旗标，并允许在不使用时隐藏，在需要建立学习队列时快速展开。
- 必须保留现有业务逻辑、AnkiConnect API 集成、表单行为、数据结构、搜索语法、路由与本地部署边界。
- 项目是无构建步骤的静态 HTML、CSS 和 JavaScript 应用；UI 改造不应无故引入框架或改变数据所有权。
- 远程可用性依赖本地服务与反向代理；Anki 不在远程服务器上运行。

## Brand Commitments

- 产品名称为 Ankimo，核心身份是 Anki 与轻量笔记记录的结合。
- 产品语言应直接、克制并面向实际操作，避免大量装饰性 emoji；红旗、橙旗、绿旗的颜色语义需要保留。
- 产品不是营销落地页，而是需要高频操作、长期使用的个人工具。

## Evidence on Hand

- `index.html`、`style.css` 和 `app.js` 构成当前可运行界面及交互实现。
- `app.js` 中已有实际 AnkiConnect 调用、短笔记暂停验证、标签筛选、旗标筛选、同步、编辑和错误反馈逻辑。
- `README.md` 记录了本地 AnkiConnect、反向代理和远程访问方式。
- 当前没有需要沿用的正式品牌素材、营销证明或对外商业声明；后续设计不得虚构这些内容。

## Product Principles

1. **标签优先。** 长期组织、筛选和学习入口围绕标签设计，牌组只在必要时出现。
2. **先记录，再整理。** 默认路径必须让短笔记快速落地，标签可以在记录时或之后逐步补充。
3. **复习语义清晰。** 短笔记默认不进入复习；问答卡片正常复习，界面必须让这一区别容易理解。
4. **Anki 是事实来源。** Ankimo 改善浏览和记录体验，但不复制或取代 Anki 的数据与复习系统。
5. **次级能力按需出现。** 旗标、牌组、高级设置和统计应可访问，但不能压过记录、标签和笔记流。
